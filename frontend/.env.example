# Backend API URL (e.g. https://your-api.onrender.com)
VITE_API_URL=
# Optional alias for backend origin used in docs/integration notes.
VITE_BACKEND_URL=

# Frontend Sentry (optional in local dev).
VITE_SENTRY_ENABLED=false
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
VITE_SENTRY_RELEASE=

# Mock mode: set to true to use demo JSON (dev only). Copy to .env.local
# VITE_USE_MOCKS=true
