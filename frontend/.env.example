# Backend API URL (e.g. https://your-api.onrender.com) - preferred, uses batch API
VITE_API_URL=

# Base URL of the TMDb proxy Worker (e.g. https://tmdb-proxy.xxx.workers.dev) - fallback
VITE_TMDB_PROXY_BASE=

# Mock mode: set to true to use demo JSON (dev only). Copy to .env.local
# VITE_USE_MOCKS=true
